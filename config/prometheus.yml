# Prometheus config - run locally: prometheus --config.file=config/prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "litellm-gateway"
    static_configs:
      - targets: ["localhost:8008"]
    metrics_path: /metrics
